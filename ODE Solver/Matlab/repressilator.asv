
%parameter array 
% [alpha, alpha_0, beta, n]

pars = [216, 0, 0.2, 2.0];
%initial conditions 
%  [m1, m2, m3, p1, p2, p3]
y0 = [0 ; 0; 0; 2; 0; 0];
t_span = [0 ; 100];

%create derivatives function
function output = derivatives(t, y, p)

%state variables 
%(m1 , m2 , m3, p1, p2, p3)
m1 = y(1);
m2 = y(2);
m3 = y(3);
p1 = y(4);
p2 = y(5);
p3 = y(6);

%parameters
alpha = p(1);
alpha_0 = p(2);
beta = p(3);
n = p(4);

    